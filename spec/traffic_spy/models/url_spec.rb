require 'spec_helper'

describe TrafficSpy::Url do 
  context "Creating" do
    let (:subject) {described_class}
    # let (:sample_data) {{url: => "http://jumpstartlab.com", :requests => 2}}

    describe ".save" do
      it "stores the information that is in the url object in a table" do
        # empty the database, see if database is empty. save. check that the database has new data
      end
    end

  end
end